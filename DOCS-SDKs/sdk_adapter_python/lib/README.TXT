LIGHTSTREAMER - PYTHON ADAPTERS - USAGE INSTRUCTIONS
====================================================

The library is deployed on the Python Package Index at https://pypi.python.org/pypi?:action=display&name=lightstreamer_adapter

To include the library in a custom project it is necessary to install the package with the following command:
pip install --pre lightstreamer-adapter

